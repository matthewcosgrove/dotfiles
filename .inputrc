# Allow up/down autocompletion of commands from bash history.
# From: coderwall.com/p/oqtj8w
# Some people prefer the non-incremental style of history completion, as opposed to the incremental style offered by C-r and C-s.
# This is the style of history completion offered by csh. bash offers bindings for this, but they are unbound by default.
"\e[A": history-search-backward
"\e[B": history-search-forward

# no longer have to hit the <Tab> key twice to produce a list of all possible completions. A single <Tab> will suffice.
set show-all-if-ambiguous on

set completion-ignore-case on

# Uncomment to enable vi-like command line editing mode.
# set editing-mode vi
